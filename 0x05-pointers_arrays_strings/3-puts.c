#include "main.h"
#include <string.h>

/**
 * _puts - prints a string to standard output
 * @str: pointer to string
 * Return: void
 */

void _puts(char *str)
{
	int count = 0;

	while (str[count] != '\0')
		_putchar(str[count];
		count++;
	_putchar('\n');
}
